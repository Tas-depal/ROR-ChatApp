<div class="col-md-12 text-center d-flex flex-column justify-content-center align-items-center" style="min-height: 70vh !important;">
        <h1>404</h1>
        <h2>Page Not Found</h2>
        <p>
            Sorry, the page you are looking
            for does not exist.
        </p>
        <%channel = Channel.find_by(channel_name: current_user.username)%>
        <%= link_to 'Back', channel_path(channel.id), class: "text-white text-decoration-none px-4 py-3 bg-success d-inline-block mt-2 rounded" %>
    </div>